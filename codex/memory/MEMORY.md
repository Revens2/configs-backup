# Codex memory index (sanitized)

The live Codex memory directory is intentionally not copied wholesale because several notes
contain credentials, private infrastructure details, historical tokens, and machine-specific
paths. This public backup keeps only the reusable Codex instructions and configuration.

To restore private memory, copy it locally from a separately protected backup; never add it to
this public repository.
